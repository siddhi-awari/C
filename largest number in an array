#include<stdio.h>
int large(int a[], int n)
{
int i,p;
p=a[0];
for(i=1;i<=n-1;i++)
{
if(p<a[i])
{
p=a[i];
}
}
printf("%d",p);
}
int main()
{
int a[20],n,i;
printf("enter: ");
scanf("%d", &n);
printf("enter: ");
for(i=0;i<=n-1;i++)
{
scanf("%d", &a[i]);
}
large(a,n);
}
