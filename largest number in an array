#include<stdio.h>
int largest(int a[], int n)
{
int i,large;
large=a[0];
for(i=1;i<=n-1;i++)
{
if(large<a[i])
{
large=a[i];
}
}
printf("%d",large);
}
int main()
{
int a[20],n,i;
printf("enter: ");
scanf("%d", &n);
printf("enter: ");
for(i=0;i<=n-1;i++)
{
scanf("%d", &a[i]);
}
largest(a,n);
}
